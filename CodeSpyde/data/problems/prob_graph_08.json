{
  "id": "prob_graph_08",
  "title": "Network Delay Time",
  "document_type": "problem",
  "topic": "Graphs",
  "subtopic": "Shortest Path",
  "pattern": "Heap / Top K",
  "difficulty": "Medium",
  "language": "python",
  "problem": "Find the minimum time it takes for all n nodes to receive a signal from source node k.",
  "constraints": "1 <= n <= 100",
  "examples": [
    {
      "input": "times = [[2,1,1],[2,3,1],[3,4,1]], n = 4, k = 2",
      "output": "2"
    }
  ],
  "expected_input": "times: list[list[int]], n: int, k: int",
  "expected_output": "int",
  "edge_cases": [
    "Unreachable nodes exist"
  ],
  "hints": [
    "Use Dijkstra's algorithm to find the shortest path from k to all other nodes."
  ],
  "expected_approach": "Dijkstra shortest path algorithm.",
  "time_complexity": "O(E log V)",
  "space_complexity": "O(V + E)",
  "related_concepts": [
    "concept_graphs",
    "concept_heaps"
  ],
  "related_mistakes": [],
  "code": "import heapq\ndef solution(times, n, k):\n    adj = [[] for _ in range(n + 1)]\n    for u, v, w in times:\n        adj[u].append((v, w))\n    dist = {i: float('inf') for i in range(1, n + 1)}\n    dist[k] = 0\n    h = [(0, k)]\n    while h:\n        d, node = heapq.heappop(h)\n        if d > dist[node]: continue\n        for neighbor, weight in adj[node]:\n            if dist[node] + weight < dist[neighbor]:\n                dist[neighbor] = dist[node] + weight\n                heapq.heappush(h, (dist[neighbor], neighbor))\n    ans = max(dist.values())\n    return ans if ans != float('inf') else -1"
}