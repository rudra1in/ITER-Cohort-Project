{
  "id": "prob_dp_11",
  "title": "Maximal Square Area",
  "document_type": "problem",
  "topic": "Dynamic Programming",
  "subtopic": "Grid Paths",
  "pattern": "Grid DP",
  "difficulty": "Medium",
  "language": "python",
  "problem": "Given an m x n binary matrix filled with 0's and 1's, find the largest square containing only 1's and return its area.",
  "constraints": "Matrix dimensions up to 300.",
  "examples": [
    {
      "input": "matrix = [['1','0'],['1','1']]",
      "output": "1"
    }
  ],
  "expected_input": "matrix: list[list[str]]",
  "expected_output": "int",
  "edge_cases": [
    "No '1's present",
    "Single cell matrix"
  ],
  "hints": [
    "State dp[i][j] holds max square side length ending at cell. Transition: if cell is 1, min(top, left, top-left) + 1."
  ],
  "expected_approach": "2D Grid DP tabulation.",
  "time_complexity": "O(m * n)",
  "space_complexity": "O(m * n) or O(n)",
  "related_concepts": [
    "concept_dp"
  ],
  "related_mistakes": [],
  "code": "def solution(matrix):\n    if not matrix: return 0\n    m, n = len(matrix), len(matrix[0])\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    max_side = 0\n    for r in range(1, m + 1):\n        for c in range(1, n + 1):\n            if matrix[r-1][c-1] == '1':\n                dp[r][c] = min(dp[r-1][c], dp[r][c-1], dp[r-1][c-1]) + 1\n                max_side = max(max_side, dp[r][c])\n    return max_side ** 2"
}