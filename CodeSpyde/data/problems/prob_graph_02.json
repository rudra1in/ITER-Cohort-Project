{
  "id": "prob_graph_02",
  "title": "Clone Graph",
  "document_type": "problem",
  "topic": "Graphs",
  "subtopic": "Graph Operations",
  "pattern": "DFS",
  "difficulty": "Medium",
  "language": "python",
  "problem": "Given a reference of a node in a connected undirected graph. Return a deep copy (clone) of the graph.",
  "constraints": "Node.val <= 100",
  "examples": [
    {
      "input": "Graph node reference",
      "output": "Cloned node reference"
    }
  ],
  "expected_input": "node: Optional[Node]",
  "expected_output": "Optional[Node]",
  "edge_cases": [
    "Empty graph",
    "Graph with cycles"
  ],
  "hints": [
    "Use a hash map to map original nodes to their cloned counterparts to avoid infinite loops in cycles."
  ],
  "expected_approach": "DFS traversal with clone dictionary lookup.",
  "time_complexity": "O(V + E)",
  "space_complexity": "O(V)",
  "related_concepts": [
    "concept_graphs",
    "concept_hash_tables"
  ],
  "related_mistakes": [
    "mistake_visited_set"
  ],
  "code": "def solution(node):\n    if not node:\n        return None\n    class Node:\n        def __init__(self, val=0, neighbors=None):\n            self.val = val\n            self.neighbors = neighbors if neighbors is not None else []\n    cloned = {}\n    def dfs(curr):\n        if curr in cloned:\n            return cloned[curr]\n        copy = Node(curr.val)\n        cloned[curr] = copy\n        for n in curr.neighbors:\n            copy.neighbors.append(dfs(n))\n        return copy\n    return dfs(node)"
}