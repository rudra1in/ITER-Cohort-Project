{
  "id": "prob_stackqueue_04",
  "title": "Evaluate Reverse Polish Notation",
  "document_type": "problem",
  "topic": "Stacks",
  "subtopic": "Parsing",
  "pattern": "Stack-based Traversal",
  "difficulty": "Medium",
  "language": "python",
  "problem": "Evaluate the value of an arithmetic expression in Reverse Polish Notation. Valid operators are +, -, *, and /.",
  "constraints": "The input is valid.",
  "examples": [
    {
      "input": "tokens = ['2','1','+','3','*']",
      "output": "9"
    }
  ],
  "expected_input": "tokens: list[str]",
  "expected_output": "int",
  "edge_cases": [
    "Negative results",
    "Integer division truncation towards zero"
  ],
  "hints": [
    "Use a stack. Push numbers. Pop two numbers and perform operation when operator is seen."
  ],
  "expected_approach": "Post-order token stack evaluation.",
  "time_complexity": "O(n)",
  "space_complexity": "O(n)",
  "related_concepts": [
    "concept_stacks"
  ],
  "related_mistakes": [
    "mistake_division_zero"
  ],
  "code": "def solution(tokens):\n    stack = []\n    for t in tokens:\n        if t in {'+', '-', '*', '/'}:\n            b = stack.pop()\n            a = stack.pop()\n            if t == '+': stack.append(a + b)\n            elif t == '-': stack.append(a - b)\n            elif t == '*': stack.append(a * b)\n            elif t == '/': stack.append(int(a / b))\n        else:\n            stack.append(int(t))\n    return stack[0]"
}