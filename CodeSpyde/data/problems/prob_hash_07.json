{
  "id": "prob_hash_07",
  "title": "Longest Consecutive Sequence",
  "document_type": "problem",
  "topic": "Hash Tables",
  "subtopic": "Sequence Search",
  "pattern": "Hash Map Lookup",
  "difficulty": "Medium",
  "language": "python",
  "problem": "Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence.",
  "constraints": "0 <= nums.length <= 10^5",
  "examples": [
    {
      "input": "nums = [100, 4, 200, 1, 3, 2]",
      "output": "4"
    }
  ],
  "expected_input": "nums: list[int]",
  "expected_output": "int",
  "edge_cases": [
    "Empty array",
    "Duplicate elements in consecutive sequences"
  ],
  "hints": [
    "Place elements in a set.",
    "Only start building sequence from a number if its predecessor is not in the set."
  ],
  "expected_approach": "Set membership check to build contiguous sequences.",
  "time_complexity": "O(n)",
  "space_complexity": "O(n)",
  "related_concepts": [
    "concept_hash_tables"
  ],
  "related_mistakes": [],
  "code": "def solution(nums):\n    num_set = set(nums)\n    longest = 0\n    for n in num_set:\n        if n - 1 not in num_set:\n            curr = n\n            streak = 1\n            while curr + 1 in num_set:\n                curr += 1\n                streak += 1\n            longest = max(longest, streak)\n    return longest"
}