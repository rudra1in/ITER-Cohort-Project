{
  "id": "prob_tree_13",
  "title": "Binary Tree Maximum Path Sum",
  "document_type": "problem",
  "topic": "Binary Trees",
  "subtopic": "Path Search",
  "pattern": "Tree DFS",
  "difficulty": "Hard",
  "language": "python",
  "problem": "A path in a binary tree is a sequence of nodes where each pair of adjacent nodes has an edge. Return the maximum path sum of any non-empty path.",
  "constraints": "Number of nodes is up to 3 * 10^4.",
  "examples": [
    {
      "input": "root = [-10, 9, 20, null, null, 15, 7]",
      "output": "42"
    }
  ],
  "expected_input": "root: Optional[TreeNode]",
  "expected_output": "int",
  "edge_cases": [
    "All negative values in tree",
    "Single node tree"
  ],
  "hints": [
    "For each node, compute max path sum containing left child and right child. Propagate only the max single path up."
  ],
  "expected_approach": "Recursive subtree maximum sums tracking.",
  "time_complexity": "O(n)",
  "space_complexity": "O(h)",
  "related_concepts": [
    "concept_binary_trees",
    "concept_recursion"
  ],
  "related_mistakes": [],
  "code": "def solution(root):\n    max_sum = float('-inf')\n    def get_max(node):\n        nonlocal max_sum\n        if not node:\n            return 0\n        left = max(get_max(node.left), 0)\n        right = max(get_max(node.right), 0)\n        curr_sum = node.val + left + right\n        max_sum = max(max_sum, curr_sum)\n        return node.val + max(left, right)\n    get_max(root)\n    return max_sum"
}