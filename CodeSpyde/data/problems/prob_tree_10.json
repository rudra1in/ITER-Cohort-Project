{
  "id": "prob_tree_10",
  "title": "Binary Tree Right Side View",
  "document_type": "problem",
  "topic": "Binary Trees",
  "subtopic": "Tree Traversal",
  "pattern": "Tree BFS",
  "difficulty": "Medium",
  "language": "python",
  "problem": "Given the root of a binary tree, imagine yourself standing on the right side of it, return the values of the nodes you can see ordered from top to bottom.",
  "constraints": "Number of nodes is up to 100.",
  "examples": [
    {
      "input": "root = [1, 2, 3, null, 5]",
      "output": "[1, 3, 5]"
    }
  ],
  "expected_input": "root: Optional[TreeNode]",
  "expected_output": "list[int]",
  "edge_cases": [
    "Left branch is longer than right branch",
    "Empty tree"
  ],
  "hints": [
    "Use Level Order Traversal (BFS) and capture the last element of each level."
  ],
  "expected_approach": "Level-order traversal storing tail elements.",
  "time_complexity": "O(n)",
  "space_complexity": "O(w)",
  "related_concepts": [
    "concept_binary_trees",
    "concept_queues"
  ],
  "related_mistakes": [],
  "code": "from collections import deque\ndef solution(root):\n    if not root:\n        return []\n    res = []\n    q = deque([root])\n    while q:\n        level_sz = len(q)\n        for i in range(level_sz):\n            node = q.popleft()\n            if i == level_sz - 1:\n                res.append(node.val)\n            if node.left: q.append(node.left)\n            if node.right: q.append(node.right)\n    return res"
}