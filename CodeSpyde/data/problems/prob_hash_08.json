{
  "id": "prob_hash_08",
  "title": "Isomorphic Strings",
  "document_type": "problem",
  "topic": "Hash Tables",
  "subtopic": "Character Mapping",
  "pattern": "Hash Map Lookup",
  "difficulty": "Easy",
  "language": "python",
  "problem": "Given two strings s and t, determine if they are isomorphic. Two strings s and t are isomorphic if the characters in s can be replaced to get t.",
  "constraints": "1 <= s.length <= 5 * 10^4",
  "examples": [
    {
      "input": "s = 'egg', t = 'add'",
      "output": "true"
    }
  ],
  "expected_input": "s: str, t: str",
  "expected_output": "bool",
  "edge_cases": [
    "Strings differ in length",
    "Duplicate mapping mismatch"
  ],
  "hints": [
    "Map each character of s to t, and each character of t to s to ensure bijection."
  ],
  "expected_approach": "Dual hash map bijection tracking.",
  "time_complexity": "O(n)",
  "space_complexity": "O(1) alphabet is limited",
  "related_concepts": [
    "concept_strings",
    "concept_hash_tables"
  ],
  "related_mistakes": [],
  "code": "def solution(s, t):\n    if len(s) != len(t):\n        return False\n    map_st, map_ts = {}, {}\n    for c1, c2 in zip(s, t):\n        if c1 in map_st and map_st[c1] != c2:\n            return False\n        if c2 in map_ts and map_ts[c2] != c1:\n            return False\n        map_st[c1] = c2\n        map_ts[c2] = c1\n    return True"
}