{
  "id": "prob_heap_08",
  "title": "Swim in Rising Water",
  "document_type": "problem",
  "topic": "Heaps",
  "subtopic": "Grid Path",
  "pattern": "Heap / Top K",
  "difficulty": "Hard",
  "language": "python",
  "problem": "Find the minimum time to reach the bottom right cell from top left cell on a grid rising water levels.",
  "constraints": "n == grid.length == grid[i].length\n1 <= n <= 50",
  "examples": [
    {
      "input": "grid = [[0, 2], [1, 3]]",
      "output": "3"
    }
  ],
  "expected_input": "grid: list[list[int]]",
  "expected_output": "int",
  "edge_cases": [
    "Grid of size 1"
  ],
  "hints": [
    "Use Dijkstra's algorithm. Maintain a min-heap of cells sorted by water height level required to reach them."
  ],
  "expected_approach": "Dijkstra min-heap path search.",
  "time_complexity": "O(n^2 log n)",
  "space_complexity": "O(n^2)",
  "related_concepts": [
    "concept_heaps",
    "concept_graphs"
  ],
  "related_mistakes": [],
  "code": "import heapq\ndef solution(grid):\n    n = len(grid)\n    visited = {(0, 0)}\n    h = [(grid[0][0], 0, 0)]\n    ans = 0\n    while h:\n        val, r, c = heapq.heappop(h)\n        ans = max(ans, val)\n        if r == n - 1 and c == n - 1:\n            return ans\n        for nr, nc in [(r+1, c), (r-1, c), (r, c+1), (r, c-1)]:\n            if 0 <= nr < n and 0 <= nc < n and (nr, nc) not in visited:\n                visited.add((nr, nc))\n                heapq.heappush(h, (grid[nr][nc], nr, nc))\n    return ans"
}