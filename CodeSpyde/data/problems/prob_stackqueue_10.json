{
  "id": "prob_stackqueue_10",
  "title": "Largest Rectangle in Histogram",
  "document_type": "problem",
  "topic": "Stacks",
  "subtopic": "Area Optimization",
  "pattern": "Monotonic Stack",
  "difficulty": "Hard",
  "language": "python",
  "problem": "Given an array of integers heights representing the histogram's bar height where the width of each bar is 1, find the area of the largest rectangle in the histogram.",
  "constraints": "1 <= heights.length <= 10^5",
  "examples": [
    {
      "input": "heights = [2, 1, 5, 6, 2, 3]",
      "output": "10"
    }
  ],
  "expected_input": "heights: list[int]",
  "expected_output": "int",
  "edge_cases": [
    "Strictly increasing heights",
    "Strictly decreasing heights",
    "Single element array"
  ],
  "hints": [
    "Use a stack to maintain index of bars in increasing order.",
    "When a shorter bar is encountered, pop elements to calculate rectangle areas."
  ],
  "expected_approach": "Monotonic stack boundaries computation.",
  "time_complexity": "O(n)",
  "space_complexity": "O(n)",
  "related_concepts": [
    "concept_stacks"
  ],
  "related_mistakes": [],
  "code": "def solution(heights):\n    stack = []\n    max_area = 0\n    heights = heights + [0]\n    for i, h in enumerate(heights):\n        while stack and heights[stack[-1]] > h:\n            height = heights[stack.pop()]\n            width = i if not stack else i - stack[-1] - 1\n            max_area = max(max_area, height * width)\n        stack.append(i)\n    return max_area"
}