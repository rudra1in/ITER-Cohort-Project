{
  "id": "sol_backtrack_09",
  "problem_id": "prob_backtrack_09",
  "document_type": "solution",
  "title": "Generate Subsets II - Solution",
  "approach": "Sort + duplicate checking backtracking.",
  "intuition": "Given an integer array nums that may contain duplicates, return all possible subsets. The solution set must not contain duplicate subsets.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Sort + duplicate checking backtracking.",
  "code": "def solution(nums):\n    nums.sort()\n    res = []\n    def backtrack(idx, path):\n        res.append(list(path))\n        for i in range(idx, len(nums)):\n            if i > idx and nums[i] == nums[i-1]:\n                continue\n            path.append(nums[i])\n            backtrack(i + 1, path)\n            path.pop()\n    backtrack(0, [])\n    return res",
  "time_complexity": "O(2^n * n)",
  "space_complexity": "O(n)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Sort the array first. Skip duplicate elements at the same recursion depth during loop."
  ]
}