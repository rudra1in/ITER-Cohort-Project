{
  "id": "sol_tree_05",
  "problem_id": "prob_tree_05",
  "document_type": "solution",
  "title": "Subtree of Another Tree - Solution",
  "approach": "Recursive root inspection with helper same-tree function.",
  "intuition": "Given the roots of two binary trees root and subRoot, return true if there is a subtree of root with the same structure and node values of subRoot.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Recursive root inspection with helper same-tree function.",
  "code": "def solution(root, subRoot):\n    def isSame(p, q):\n        if not p and not q:\n            return True\n        if not p or not q:\n            return False\n        return p.val == q.val and isSame(p.left, q.left) and isSame(p.right, q.right)\n    if not root:\n        return False\n    if isSame(root, subRoot):\n        return True\n    return solution(root.left, subRoot) or solution(root.right, subRoot)",
  "time_complexity": "O(n * m) worst case",
  "space_complexity": "O(h)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Check if root is same tree as subRoot, or if subRoot is subtree of root.left or root.right."
  ]
}