{
  "id": "sol_tree_04",
  "problem_id": "prob_tree_04",
  "document_type": "solution",
  "title": "Symmetric Tree Checking - Solution",
  "approach": "Mirror helper comparison.",
  "intuition": "Given the root of a binary tree, check whether it is a mirror of itself (i.e., symmetric around its center).",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Mirror helper comparison.",
  "code": "def solution(root):\n    if not root:\n        return True\n    def isMirror(t1, t2):\n        if not t1 and not t2:\n            return True\n        if not t1 or not t2:\n            return False\n        return t1.val == t2.val and isMirror(t1.left, t2.right) and isMirror(t1.right, t2.left)\n    return isMirror(root.left, root.right)",
  "time_complexity": "O(n)",
  "space_complexity": "O(h)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Compare left subtree with mirror of right subtree recursively."
  ]
}