{
  "id": "sol_dp_04",
  "problem_id": "prob_dp_04",
  "document_type": "solution",
  "title": "Longest Increasing Subsequence Length - Solution",
  "approach": "O(n^2) DP loop.",
  "intuition": "Given an integer array nums, return the length of the longest strictly increasing subsequence.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "O(n^2) DP loop.",
  "code": "def solution(nums):\n    if not nums: return 0\n    dp = [1] * len(nums)\n    for i in range(len(nums)):\n        for j in range(i):\n            if nums[i] > nums[j]:\n                dp[i] = max(dp[i], dp[j] + 1)\n    return max(dp)",
  "time_complexity": "O(n^2)",
  "space_complexity": "O(n)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "State dp[i] holds LIS length ending at index i. Compare with all elements before index i."
  ]
}