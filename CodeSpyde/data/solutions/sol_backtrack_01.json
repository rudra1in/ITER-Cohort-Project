{
  "id": "sol_backtrack_01",
  "problem_id": "prob_backtrack_01",
  "document_type": "solution",
  "title": "Subsets Generation - Solution",
  "approach": "Decision tree backtracking traversal.",
  "intuition": "Given an integer array nums of unique elements, return all possible subsets (the power set).",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Decision tree backtracking traversal.",
  "code": "def solution(nums):\n    res = []\n    def backtrack(idx, path):\n        res.append(list(path))\n        for i in range(idx, len(nums)):\n            path.append(nums[i])\n            backtrack(i + 1, path)\n            path.pop()\n    backtrack(0, [])\n    return res",
  "time_complexity": "O(2^n * n)",
  "space_complexity": "O(n)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use backtracking. At each index, decide whether to include the element in the subset or not."
  ]
}