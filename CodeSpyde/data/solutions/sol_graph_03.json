{
  "id": "sol_graph_03",
  "problem_id": "prob_graph_03",
  "document_type": "solution",
  "title": "Course Schedule Course Ordering - Solution",
  "approach": "Kahn's algorithm in-degree BFS queue.",
  "intuition": "There are a total of numCourses courses you have to take, labeled from 0 to numCourses - 1. You are given an array prerequisites. Return true if you can finish all courses.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Kahn's algorithm in-degree BFS queue.",
  "code": "from collections import deque\ndef solution(numCourses, prerequisites):\n    adj = [[] for _ in range(numCourses)]\n    indegree = [0] * numCourses\n    for dest, src in prerequisites:\n        adj[src].append(dest)\n        indegree[dest] += 1\n    q = deque([i for i in range(numCourses) if indegree[i] == 0])\n    visited = 0\n    while q:\n        node = q.popleft()\n        visited += 1\n        for neighbor in adj[node]:\n            indegree[neighbor] -= 1\n            if indegree[neighbor] == 0:\n                q.append(neighbor)\n    return visited == numCourses",
  "time_complexity": "O(V + E)",
  "space_complexity": "O(V + E)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use topological sort. Track in-degree of nodes. If cycle exists, topological sort won't visit all courses."
  ]
}