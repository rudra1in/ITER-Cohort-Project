{
  "id": "sol_linkedlist_03",
  "problem_id": "prob_linkedlist_03",
  "document_type": "solution",
  "title": "Linked List Cycle Detection - Solution",
  "approach": "Floyd's cycle detection algorithm.",
  "intuition": "Given head, the head of a linked list, determine if the linked list has a cycle in it.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Floyd's cycle detection algorithm.",
  "code": "def solution(head):\n    slow = head\n    fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow == fast:\n            return True\n    return False",
  "time_complexity": "O(n)",
  "space_complexity": "O(1)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use fast and slow pointers. If they meet, there is a cycle."
  ]
}