{
  "id": "sol_graph_09",
  "problem_id": "prob_graph_09",
  "document_type": "solution",
  "title": "Cheapest Flights Within K Stops - Solution",
  "approach": "Iterative state distance updates.",
  "intuition": "Find cheapest price from src to dst with at most k stops.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Iterative state distance updates.",
  "code": "def solution(n, flights, src, dst, k):\n    prices = [float('inf')] * n\n    prices[src] = 0\n    for _ in range(k + 1):\n        temp = list(prices)\n        for u, v, w in flights:\n            if prices[u] != float('inf') and prices[u] + w < temp[v]:\n                temp[v] = prices[u] + w\n        prices = temp\n    return prices[dst] if prices[dst] != float('inf') else -1",
  "time_complexity": "O(k * E)",
  "space_complexity": "O(V)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use Bellman-Ford algorithm with K+1 relaxation limit iterations."
  ]
}