{
  "id": "sol_searchsort_02",
  "problem_id": "prob_searchsort_02",
  "document_type": "solution",
  "title": "Search in Rotated Sorted Array - Solution",
  "approach": "Pivot-aware binary search.",
  "intuition": "Given a rotated sorted array of unique elements and a target, search target and return its index.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Pivot-aware binary search.",
  "code": "def solution(nums, target):\n    l, r = 0, len(nums) - 1\n    while l <= r:\n        mid = l + (r - l) // 2\n        if nums[mid] == target: return mid\n        if nums[l] <= nums[mid]:\n            if nums[l] <= target < nums[mid]:\n                r = mid - 1\n            else:\n                l = mid + 1\n        else:\n            if nums[mid] < target <= nums[r]:\n                l = mid + 1\n            else:\n                r = mid - 1\n    return -1",
  "time_complexity": "O(log n)",
  "space_complexity": "O(1)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Identify which half of the array is sorted at the mid index. Perform range checking on target to discard half search space."
  ]
}