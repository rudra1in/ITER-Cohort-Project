{
  "id": "sol_graph_14",
  "problem_id": "prob_graph_14",
  "document_type": "solution",
  "title": "Reconstruct Itinerary - Solution",
  "approach": "Eulerian DFS post-order accumulation.",
  "intuition": "Reconstruct the itinerary in order. Flight must start from JFK.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Eulerian DFS post-order accumulation.",
  "code": "def solution(tickets):\n    adj = {}\n    for u, v in tickets:\n        if u not in adj: adj[u] = []\n        adj[u].append(v)\n    for u in adj:\n        adj[u].sort(reverse=True)\n    route = []\n    def dfs(airport):\n        while adj.get(airport):\n            next_dest = adj[airport].pop()\n            dfs(next_dest)\n        route.append(airport)\n    dfs('JFK')\n    return route[::-1]",
  "time_complexity": "O(E log (E/V))",
  "space_complexity": "O(V + E)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use Hierholzer's algorithm for Eulerian Path. Sort destinations lexically."
  ]
}