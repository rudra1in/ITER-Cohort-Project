{
  "id": "sol_graph_12",
  "problem_id": "prob_graph_12",
  "document_type": "solution",
  "title": "Course Schedule II - Solution",
  "approach": "Kahn's algorithm in-degree BFS queue.",
  "intuition": "Return the ordering of courses you should take to finish all courses. If impossible, return an empty array.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Kahn's algorithm in-degree BFS queue.",
  "code": "from collections import deque\ndef solution(numCourses, prerequisites):\n    adj = [[] for _ in range(numCourses)]\n    indegree = [0] * numCourses\n    for dest, src in prerequisites:\n        adj[src].append(dest)\n        indegree[dest] += 1\n    q = deque([i for i in range(numCourses) if indegree[i] == 0])\n    order = []\n    while q:\n        node = q.popleft()\n        order.append(node)\n        for neighbor in adj[node]:\n            indegree[neighbor] -= 1\n            if indegree[neighbor] == 0:\n                q.append(neighbor)\n    return order if len(order) == numCourses else []",
  "time_complexity": "O(V + E)",
  "space_complexity": "O(V + E)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use Kahn's algorithm or DFS topological sort. Add nodes to output when their in-degree becomes 0."
  ]
}