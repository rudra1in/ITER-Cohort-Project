{
  "id": "sol_stackqueue_02",
  "problem_id": "prob_stackqueue_02",
  "document_type": "solution",
  "title": "Min Stack - Solution",
  "approach": "Synchronized dual-stack tracking.",
  "intuition": "Design a stack that supports push, pop, top, and retrieving the minimum element in constant time. Return min value from a series of operations.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Synchronized dual-stack tracking.",
  "code": "def solution(operations):\n    stack = []\n    min_stack = []\n    res = []\n    for op in operations:\n        cmd = op[0]\n        if cmd == 'push':\n            val = op[1]\n            stack.append(val)\n            if not min_stack or val <= min_stack[-1]:\n                min_stack.append(val)\n        elif cmd == 'pop':\n            if stack:\n                val = stack.pop()\n                if val == min_stack[-1]:\n                    min_stack.pop()\n        elif cmd == 'getMin':\n            res.append(min_stack[-1] if min_stack else None)\n    return res",
  "time_complexity": "O(1) per operation",
  "space_complexity": "O(n)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use an auxiliary stack to keep track of the minimum value at each push state."
  ]
}