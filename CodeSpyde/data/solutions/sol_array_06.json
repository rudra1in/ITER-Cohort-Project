{
  "id": "sol_array_06",
  "problem_id": "prob_array_06",
  "document_type": "solution",
  "title": "Move Zeroes to End - Solution",
  "approach": "Two pointers: scan non-zeroes and swap them to the front.",
  "intuition": "Given an integer array nums, move all 0's to the end of it while maintaining the relative order of the non-zero elements.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Two pointers: scan non-zeroes and swap them to the front.",
  "code": "def solution(nums):\n    last_non_zero = 0\n    for i in range(len(nums)):\n        if nums[i] != 0:\n            nums[last_non_zero], nums[i] = nums[i], nums[last_non_zero]\n            last_non_zero += 1\n    return nums",
  "time_complexity": "O(n)",
  "space_complexity": "O(1)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use a pointer to track the position of the next non-zero element."
  ]
}