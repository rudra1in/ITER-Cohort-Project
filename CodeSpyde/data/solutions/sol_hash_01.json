{
  "id": "sol_hash_01",
  "problem_id": "prob_hash_01",
  "document_type": "solution",
  "title": "Two Sum Problem - Solution",
  "approach": "Single pass complement matching using dictionary lookup.",
  "intuition": "Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Single pass complement matching using dictionary lookup.",
  "code": "def solution(nums, target):\n    seen = {}\n    for i, n in enumerate(nums):\n        comp = target - n\n        if comp in seen:\n            return [seen[comp], i]\n        seen[n] = i\n    return []",
  "time_complexity": "O(n)",
  "space_complexity": "O(n)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Use a hash map to map each element to its index."
  ]
}