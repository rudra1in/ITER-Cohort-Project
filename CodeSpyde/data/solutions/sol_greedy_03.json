{
  "id": "sol_greedy_03",
  "problem_id": "prob_greedy_03",
  "document_type": "solution",
  "title": "Gas Station Circuit - Solution",
  "approach": "Single pass greedy start tracker.",
  "intuition": "Find start index of gas station to complete circuit once, else return -1.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Single pass greedy start tracker.",
  "code": "def solution(gas, cost):\n    if sum(gas) < sum(cost): return -1\n    tank = 0\n    start = 0\n    for i in range(len(gas)):\n        tank += gas[i] - cost[i]\n        if tank < 0:\n            tank = 0\n            start = i + 1\n    return start",
  "time_complexity": "O(n)",
  "space_complexity": "O(1)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "If total gas is less than total cost, solution is impossible. Keep track of current gas tank status; if it goes negative, reset start index to next station."
  ]
}