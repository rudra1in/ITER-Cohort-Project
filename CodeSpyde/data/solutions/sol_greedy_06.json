{
  "id": "sol_greedy_06",
  "problem_id": "prob_greedy_06",
  "document_type": "solution",
  "title": "Hand of Straights - Solution",
  "approach": "Sorted frequency map decrement simulation.",
  "intuition": "Check if hand cards can be grouped into subsets of size groupSize containing consecutive cards.",
  "step_by_step_explanation": "Iterate through elements and apply logic accordingly.",
  "brute_force_approach": "Perform brute force nested check.",
  "optimized_approach": "Sorted frequency map decrement simulation.",
  "code": "def solution(hand, groupSize):\n    if len(hand) % groupSize != 0:\n        return False\n    count = {}\n    for card in hand:\n        count[card] = count.get(card, 0) + 1\n    sorted_keys = sorted(count.keys())\n    for card in sorted_keys:\n        if count[card] > 0:\n            needed = count[card]\n            for i in range(groupSize):\n                if count.get(card + i, 0) < needed:\n                    return False\n                count[card + i] -= needed\n    return True",
  "time_complexity": "O(n log n)",
  "space_complexity": "O(n)",
  "common_alternative_approaches": [
    "Hash map lookup alternative"
  ],
  "common_mistakes": [
    "Count card frequencies. Sort unique cards and greedily start straight groups from the smallest card available."
  ]
}